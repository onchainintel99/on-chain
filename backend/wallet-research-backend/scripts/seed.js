require("dotenv").config();
console.log("Demo seed data has been removed. The application starts with an empty database.");
console.log("Create the first admin with: npm run create-admin");
process.exit(0);
